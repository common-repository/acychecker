"use strict";

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }

var AcycDatabase = /*#__PURE__*/function () {
  function AcycDatabase() {
    _classCallCheck(this, AcycDatabase);

    this.addAlertOnDeleteUserAction();
    this.setSaveButtonCondition();
  }

  _createClass(AcycDatabase, [{
    key: "addAlertOnDeleteUserAction",
    value: function addAlertOnDeleteUserAction() {
      jQuery('#delete_users').on('click', function (event) {
        if (!confirm(ACYC_LANGUAGES.ACYC_ARE_YOUR_SURE_DELETE_USERS_CONFIRM)) {
          event.preventDefault();
        }
      });
    }
  }, {
    key: "setSaveButtonCondition",
    value: function setSaveButtonCondition() {
      jQuery.tablesSelected = function () {
        if (jQuery('[name^="acyc_config[tables_selected]"]:checked').length === 0) {
          alert(ACYC_LANGUAGES.ACYC_NO_USER_TABLE_SELECTED);
          return false;
        }

        if (!jQuery('#do_nothing').is(':checked') && jQuery('[name^="acyc_config[conditions_selected]"]:checked').length === 0) {
          alert(ACYC_LANGUAGES.ACYC_PLEASE_SELECT_A_CONDITION);
          return false;
        }

        if ('do_nothing' !== jQuery('[name="acyc_config[action_selected]"]:checked').val()) {
          if (!confirm(ACYC_LANGUAGES.ACYC_LAST_CONFIRMATION)) {
            return false;
          }
        }

        return true;
      };
    }
  }]);

  return AcycDatabase;
}();

jQuery(function () {
  new AcycDatabase();
});