"use strict";

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

var AcyChecker = function AcyChecker() {
  _classCallCheck(this, AcyChecker);

  // Builds the modals among other things
  jQuery(document).foundation();
  PaginationService.reloadFormPaginationChange();
  StatusService.initStatusChange();
  TooltipService.initTooltip();
  NoticeService.initNotice();
  ModalService.initModal();
  FormService.setSubmitButton();
  FieldsService.setSelect2();

  if ('joomla' === ACYC_CMS && !ACYC_J40) {
    JoomlaMenuService.setJoomlaLeftMenu();
  } // For both clean my database and block on registration


  DatabaseService.addAlertOnFreeDomainCheck();
};

jQuery(function () {
  new AcyChecker();
});