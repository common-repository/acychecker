"use strict";

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }

var StatusService = /*#__PURE__*/function () {
  function StatusService() {
    _classCallCheck(this, StatusService);
  }

  _createClass(StatusService, null, [{
    key: "initStatusChange",
    value: function initStatusChange() {
      var statuses = document.querySelectorAll('.acyc__listing__status__one');
      if (!statuses.length) return;
      var inputStatus = document.getElementById('acyc__listing__status');
      var form = inputStatus.closest('form');
      statuses.forEach(function (statusLink) {
        statusLink.addEventListener('click', function () {
          inputStatus.value = statusLink.getAttribute('data-acyc-status');
          form.submit();
        });
      });
    }
  }]);

  return StatusService;
}();