"use strict";

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }

var DatabaseService = /*#__PURE__*/function () {
  function DatabaseService() {
    _classCallCheck(this, DatabaseService);
  }

  _createClass(DatabaseService, null, [{
    key: "addAlertOnFreeDomainCheck",
    value: function addAlertOnFreeDomainCheck() {
      jQuery('[name="acyc_config[conditions_selected][free_domain]"], [name="acyc_config[registration_conditions][free_domain]"]').on('click', function (event) {
        if (!jQuery(this).is(':checked')) return;

        if (!confirm(ACYC_LANGUAGES.ACYC_ARE_YOUR_SURE_FREE_DOMAINS_CONFIRM)) {
          event.preventDefault();
        }
      });
    }
  }]);

  return DatabaseService;
}();