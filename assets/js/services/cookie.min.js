"use strict";

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }

var CookieService = /*#__PURE__*/function () {
  function CookieService() {
    _classCallCheck(this, CookieService);
  }

  _createClass(CookieService, null, [{
    key: "setCookie",
    value: function setCookie(cookieName, value, expiredays) {
      var newCookie = cookieName + '=' + CookieService.escape(value);

      if (expiredays != null) {
        var exdate = new Date();
        exdate.setDate(exdate.getDate() + expiredays);
        newCookie += ';expires=' + exdate.toUTCString();
      }

      document.cookie = newCookie;
    }
  }, {
    key: "escape",
    value: function escape(str) {
      str = str.replace(/"/g, '&quot;');
      return str;
    }
  }]);

  return CookieService;
}();